import 'package:cuentas_app/config/constants/app_constants.dart';
import 'package:flutter/material.dart';

class Coolors {
  Coolors._();

  static const primary = Color(colorPrimary);
  static const primaryDark = Color(colorPrimaryDark);

  static const accent = Color(colorAcent);
  static const accentDark = Color(colorAcentDark);

  static const dark = Color(colorDark);
  static const darker = Color(colorDarker);

  static const light = Color(colorLight);

  static const gray = Color(colorGray);
  static const grayLight = Color(colorGrayLight);

  static const danger = Color(colorDanger);
  static const dangerDarker = Color(colorDangerDarker);
  static const dangerLight = Color(colorDangerLight);
}
